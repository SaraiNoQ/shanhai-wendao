# AGENTS.md

本文件约束所有在《山海问道》项目中工作的开发者和 AI 代理。规则适用于本目录及全部子目录。

## 开始工作前

按顺序阅读：

1. `README.md`
2. `docs/README.md`
3. `docs/东方志怪放置卡牌_GDD_v0.1.md`
4. 与任务直接相关的规范文档

先搜索现有定义和调用方，再修改。不得在不了解主线、战斗和离线模拟共用关系时单独修补其中一条路径。

## 来源优先级

发生冲突时按以下优先级处理：

1. 用户当前明确要求
2. `docs/东方志怪放置卡牌_GDD_v0.1.md`：产品行为与原型范围
3. `docs/architecture.md`：技术边界与模块职责
4. `docs/content-schema.md`：内容字段、ID 和效果约定
5. `docs/prototype-checklist.md`：完成与验收条件

不要复制同一规则到多份文档。产品规则写入 GDD，技术决策写入架构文档，字段规则写入内容规范。

## 当前范围

- 只实现“炼气至筑基”垂直切片。
- 平台为横屏电脑网页，单机、本地存档。
- 核心是卡桌战斗、挂机主线、构筑养成和突破劫境。
- 不实现账号、服务器、付费、多人、角色自由移动、真实碰撞、完整动作系统或运行时 AI。
- 不提前制作第二境界、通用编辑器、插件系统或批量正式素材。

## 实现原则

- 使用 `docs/architecture.md` 中的最小技术栈；新增依赖前必须说明现有平台能力为何不足。
- 战斗、在线自动和离线模拟必须调用同一个纯逻辑核心，禁止维护三套近似规则。
- 时间、随机数和存档输入必须显式传入；同一随机种子与输入应产生相同结果。
- UI 只负责展示和发出意图，不在组件里计算伤害、奖励或掉落。
- 游戏状态必须可序列化；存档字段变化必须提升 `saveVersion` 并提供迁移。
- 优先使用整数和明确舍入规则，避免浮点误差让在线与离线结果分叉。
- 内容优先通过数据和已有 `effectId` 组合表达；仅当规则无法表达时再添加新效果实现。
- 不为“以后可能需要”建立抽象、工厂、服务层或配置项。

## 工程与架构规则

2. 选能满足当前需求的最简单实现。不要预防性抽象，不要多此一举的配置层。
3. 系统分层要克制。先跑通一个最小的端到端版本，再往上加东西。绝不为了未完成的复杂度拆掉能跑的东西。
4. 组件保持模块化，关注点分离。
5. 优先用成熟的、有人维护的库。没有明确理由别自己重写。
6. 先检查项目里已有的依赖能做什么，再考虑加新包或自己写；不得先假设现有库没有所需能力。
7. 架构决策按长期维护设计。不接受“先这样以后再换”的临时方案。最小端到端可以省略未实现功能，但已建立的核心边界必须能继续演进，不能以推倒重写为前提。
8. 新系统、重大依赖或架构决策开始前，先调研成熟产品和同类开源项目如何解决同一问题，优先采用已验证模式。调研必须说明候选方案解决的问题、架构、依赖、活跃度及值得复用或避开的设计，并结合本项目给出技术选型、系统架构、MVP 范围和开发顺序；得到用户确认后再实现。普通缺陷修复和既定架构内的小改动不重复进行全量调研。

## 内容与文案

- 稳定 ID 使用小写英文 `snake_case`；显示名称和玩家文案使用简体中文。
- 已发布 ID 不因改名而变化，存档和奖励来源只引用 ID。
- 卡面文字先写结果，再写条件；同一关键词必须使用同一名称。
- 卡面不承载生成式图片文字，名称、数值和描述由 UI 绘制。
- 新内容必须说明获取来源、标签、升级变化和重复转化结果。

## 变更流程

1. 确认任务属于当前垂直切片。
2. 找到唯一来源和所有消费者。
3. 完成最小修改。
4. 为分支、循环、随机、存档和结算逻辑留下最小可运行检查。
5. 运行与改动相关的检查；不要顺手格式化或改写无关文件。
6. 若玩家可见行为变化，同步更新唯一来源文档和验收项。

## Cloudflare 部署

- 生产环境使用一个 Cloudflare Worker 同时承载 Vite 静态资源和 `/api/*` 后端；不得另建 Pages 项目或重复 API 服务。
- 生产域名固定为 `https://wendao.sarainoq.cn`。`workers_dev` 和 Preview URLs 保持关闭，除非用户明确批准临时公开地址。
- 静态资源默认由 Cloudflare 直接缓存和响应；只有 `/api/*` 使用 `run_worker_first` 进入 Worker。
- Wrangler 配置以根目录 `wrangler.jsonc` 为唯一来源。不得只在 Dashboard 修改域名、路由或绑定，否则下次部署会被配置文件覆盖。
- 本地开发使用 `pnpm dev`，生产运行时预览使用 `pnpm preview`，完整部署前检查使用 `pnpm cf:check`。
- 生产部署只能从 `main` 分支的干净工作树执行：先运行 `pnpm check`，再运行 `pnpm deploy`，最后验证首页和 `/api/health`。
- 首次连接或凭据过期时运行 `pnpm cf:whoami`，仅在未登录时使用 `pnpm exec wrangler login`。
- 密钥必须通过 `pnpm exec wrangler secret put <NAME>` 或 Cloudflare Secrets Store 设置；不得写入 Git、`.env`、`wrangler.jsonc` 或文档。
- 新增 KV、D1、R2、Queues、Durable Objects 等绑定前必须先有产品需求、数据模型、迁移与故障处理方案，不预建空资源。
- `compatibility_date` 只在阅读 Cloudflare 兼容性变更并通过完整测试后更新，不随日常提交自动滚动。

## 完成标准

- 改动符合 GDD 且没有扩大范围。
- 相关检查通过；无法运行时明确说明原因。
- 没有新增无说明依赖、重复规则或不可复现随机性。
- 文档、内容数据与实现使用相同术语和 ID。
- 部署相关改动必须通过 Worker API 测试、Wrangler dry-run 和线上健康检查。

安装、开发、测试、检查和构建命令记录在 `README.md`。
